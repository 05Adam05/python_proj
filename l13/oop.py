class Secret_info:
	def __init__(self, data, parols):
		self.__data = data
		self.secret_parols = parol

	def get_info(self):
		return secret_parols

	def set_info(self, new_inforation):
		return secret_parols = new_inforation

	self.parols = property(get_secret_parols, set_secret_parols)
	
my_doc = Secret_info("2*2=4")


print(my_doc._Secret_info__data)